#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <string.h>
//#include "pdf417lib.h"
//#include <dmtx.h>
#include <qrencode.h>

#define adalogo_width  75
#define adalogo_height 75
#define DATA_MATRIX 0xff
#define QRCODE 1

static const unsigned char adalogo_data[] = {
  
};

void scaleByte(char* newBytes, char oldByte, int scale)
{
  int bitIndex = 0;
  int spotInArray = 0;
  memset(newBytes, 0, scale);
  //8 bits to a byte
  for(int i = 0; i < 8; i++) {
    //The ith bit is set
    int isSet = 0;
    if((oldByte & (1 << i)) != 0) {
        isSet = 1;
    }
    for(int j = 0; j < scale; j++) {
      newBytes[(scale - spotInArray) - 1] = newBytes[(scale - spotInArray) - 1] | (isSet << bitIndex % 8);
      bitIndex++;
      if (bitIndex % 8 == 0) {
        spotInArray++;
      }
    }
  }
}

//Scale must be less than 8!
void printBitmap(int fileDescriptor, unsigned char* input, int rows, int columns, int scale)
{
  //Need to know the number of bytes a column will take, so round up to nearest multiple of 8
  int roundedColumns = (columns + 7)/8;

  int scaledColumns = scale * roundedColumns;

  //The printer has a maximum width of 384 (8*48) pixels
  if(scaledColumns > 48) {
    printf("The image is too wide to print. Please use a value less than 48\n");
    return;
  }

  write(fileDescriptor, "\n", 1);
  
  for (int i = 0; i < rows; i++) {
    //Each row is printed scale times
    for(int j = 0; j < scale; j++) {
      
      //Tell the printer we are starting a new row
      write(fileDescriptor, "\x12", 1);
      write(fileDescriptor, "*", 1);
      //1 row at a time, so number of rows is 1
      write(fileDescriptor, "\x1", 1);
      //Always padding up to maximum width, so width is always 0x30 or 48
      write(fileDescriptor, "\x30", 1);

      for (int k = 0; k < 48; k++) {
        //Each pixel is printed scale times
        char *newBytes;
        newBytes = (char*)malloc(scale);
        scaleByte(newBytes, input[i * roundedColumns + k], scale);
        for(int l = 0; l < scale; l++) {
          //Pad any image less than 384 pixels with whitespace
          if (k >= roundedColumns) {
            write(fileDescriptor, "\x00", 1);
          }
          else {
            write(fileDescriptor, &newBytes[l], 1);  
          }
        }
        free(newBytes); 
      }
    }
  }

  write(fileDescriptor, "\n", 1);
}


// Currently not supported. Would be fairly easy to update to work again however
// void printPDF417Barcode(char *input) 
// {
//   pdf417param barcode;
//   pdf417init(&barcode);
//   barcode.text = input;
//   barcode.codeColumns = 1;
//   barcode.options = PDF417_FIXED_COLUMNS;
//   paintCode(&barcode);

//   printBitmap((unsigned char *)barcode.outBits, barcode.codeRows, barcode.bitColumns, 4);

//   pdf417free(&barcode);
//}

void make1bitBitmap(unsigned char* output, unsigned char *input, int width, int height, int bytesPerPixel, int type)
{
  int whatByte = 0;
  int whatBit = 0;

  for(int i = 0; i < height*bytesPerPixel; i += bytesPerPixel) {
    for(int j = 0; j < width*bytesPerPixel; j += bytesPerPixel) {
      //Pixel is black
      if((input[i * width + j] & type) != 0) {
        output[whatByte] += (1 << (7-whatBit));
      }
      whatBit++;
      if(whatBit % 8 == 0) {
        whatBit = 0;
        whatByte++;
      }
    }
    if(whatBit % 8 != 0) {
      whatByte++;
    }
    whatBit = 0;
  }
}

void printer_printString(int fileDescriptor, char* input)
{
  write(fileDescriptor, "\n", 1);
  for(int i = 0; i < strlen(input); i++) {
    write(fileDescriptor, input + i, 1);
  }
  write(fileDescriptor, "\n", 1);
}

void printer_printQRCode(int fileDescriptor, const unsigned char* input, int size) 
{
  unsigned char *pxl1d;
  int width;
  
  QRcode *toPrint = QRcode_encodeData(size, input, 0, QR_ECLEVEL_L);
  width = toPrint->width;

  pxl1d = (unsigned char*)malloc(width * (width + 7)/8);
  memset(pxl1d, 0, width*((width + 7)/8));
  make1bitBitmap(pxl1d, toPrint->data, width, width, 1, QRCODE);  
  printBitmap(fileDescriptor, pxl1d, width, width, 6);

  QRcode_free(toPrint);
  free(pxl1d);
}

//Currently not supported, does not need much work to get up and running again however
// void printDataMatrix(const unsigned char* input, int size)
// {
//   size_t width, height, bytesPerPixel;
//   unsigned char *pxl;
//   DmtxEncode *enc;

//   /* 1) ENCODE a new Data Matrix barcode image (in memory only) */

//   enc = dmtxEncodeCreate();
//   dmtxEncodeDataMatrix(enc, size, (unsigned char*)input);

//   /* 2) COPY the new image data before releasing encoding memory */

//   width = dmtxImageGetProp(enc->image, DmtxPropWidth);
//   height = dmtxImageGetProp(enc->image, DmtxPropHeight);
//   bytesPerPixel = dmtxImageGetProp(enc->image, DmtxPropBytesPerPixel);

//   //printf("Width %d Height %d BytesPerPixel %d\n", width, height, bytesPerPixel);

//   pxl = (unsigned char *)malloc(width * height * bytesPerPixel);
//   memcpy(pxl, enc->image->pxl, width * height * bytesPerPixel);

//   //Sadly though the library says it supports 1 bit per pixel bitmaps. If you try to use it, 
//   //it fails on an assertion. On reading the code it actually says within the code that it is 
//   //not supported. So transform the bitmap into a 1 bit per pixel bitmap
//   unsigned char *pxl1d = (unsigned char *)malloc( (width + 7)/8 * height );
//   memset(pxl1d, 0,  (width + 7)/8 * height );
//   make1bitBitmap(pxl1d, pxl, width, height, bytesPerPixel, DATA_MATRIX);
  
//   //printf("%s\n", input);
//   printBitmap(pxl1d, height,width, 1);

//   //Cleanup!
//   dmtxEncodeDestroy(&enc);
//   free(pxl);
//   free(pxl1d);
// }

void printer_printTestBitmap(int fileDescriptor)
{
  write(fileDescriptor, "\n", 1);
  
  for (int i = 0; i < adalogo_height; i++) {
    write(fd, "\x12", 1);
    write(fd, "*", 1);
    write(fd, "\x1", 1);
    write(fd, "\x30", 1);
    for (int j = 0; j < 48; j++) {
      if (j >= 10) {
        write(fileDescriptor, "\x00", 1);
      }
      else {
        write(fileDescriptor, &adalogo_data[i * 10 + j], 1);  
      } 
    }
  }
  write(fileDescriptor, "\n", 1);
}